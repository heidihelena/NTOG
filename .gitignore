# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
*~

# Python (ingest/model scripts under models/)
__pycache__/
*.pyc
.venv/
venv/

# Local-only model outputs (regenerated by model.py)
models/nordic-lc-screening/outputs/
models/nordic-lc-screening/*.png

# models/ — Nordic LC screening health-economic model. DECISION (founder, 2026-06-10):
# data too weak to publish → NOT pushed, kept purely local. Do not commit.
models/

# NTOG2027 congress programme DATA not finalised → real programme.json NOT pushed
# (founder, 2026-06-10). programme.html is a "to be announced" STUB that IS pushed so the
# global nav link keeps working; it does not load programme.json.
ntog2027/programme.json

# local-only social-banner generator (renders og-image.png); not deployed
og-image.html
og-image-2027.html
